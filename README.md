# Let's Party - Мультиязычный лендинг с Decap CMS и SSG

## 🚀 Установка и запуск

```bash
# Установка зависимостей
npm install

# Запуск dev сервера
npm run dev

# Сборка для продакшена (генерация статических HTML)
npm run build

# Превью продакшн билда
npm run preview
```

## 🎯 Как это работает (SSG - Static Site Generation)

### Принцип работы:

1. **Клиент редактирует контент** в CMS (`/admin/`)
2. **CMS делает коммит** в GitHub репозиторий
3. **Netlify автоматически запускает билд** (`npm run build`)
4. **Vite генерирует статические HTML** на основе данных из CMS
5. **Результат - 3 полностью статичных HTML** файла (RU, LV, EN) с вшитым контентом

### Преимущества SSG:

✅ **SEO-friendly** - весь контент в HTML, поисковики индексируют всё  
✅ **Быстрая загрузка** - никакого клиентского рендеринга  
✅ **Работает без JS** - базовый контент доступен всегда  
✅ **Простой деплой** - обычный статический хостинг  

### Build Process:

```
CMS изменение → Git commit → Netlify build → 
  ↓
Vite плагин читает:
  - public/locales/*.json (переводы)
  - public/reviews/*.md (отзывы)
  - public/gallery-items/*.md (галерея)
  ↓
Генерирует 3 HTML файла:
  - dist/index.html (RU)
  - dist/lv/index.html (LV)
  - dist/en/index.html (EN)
```

## 🌍 Локализация

Проект поддерживает 3 языка:
- **Русский (RU)** - `/` (по умолчанию)
- **Латышский (LV)** - `/lv/`
- **Английский (EN)** - `/en/`

Переводы хранятся в `public/locales/`:
- `ru.json`
- `lv.json`
- `en.json`

## 📝 Управление контентом через Decap CMS

### Доступ к админ-панели

После деплоя на Netlify:
1. Перейдите на `https://ваш-сайт.netlify.app/admin/`
2. Войдите через Netlify Identity

### Настройка Netlify Identity (один раз)

1. В Netlify Dashboard откройте свой проект
2. Перейдите в `Site settings` → `Identity`
3. Нажмите `Enable Identity`
4. В разделе `Registration preferences` выберите `Invite only`
5. В разделе `Services` → `Git Gateway` нажмите `Enable Git Gateway`
6. Пригласите себя: `Identity` → `Invite users`

### Что можно редактировать в CMS

#### 1. Локализации
- Заголовки секций
- Тексты кнопок
- Контент разделов "О нас", "Контакты" и т.д.

#### 2. Отзывы
- Добавление/удаление отзывов
- Текст отзыва на 3 языках
- Скриншот отзыва (загрузка файла)
- Порядок отображения

#### 3. Галерея
- Добавление/удаление фотографий
- Alt-тексты на 3 языках
- Порядок отображения

## 📁 Структура проекта

```
LetsParty/
├── build-plugins/          # 🆕 Build-time плагины
│   └── ssg-plugin.js      # Генератор статических HTML
├── public/
│   ├── admin/              # Админ-панель Decap CMS
│   │   ├── config.yml      # Конфигурация CMS
│   │   └── index.html      # Страница входа в CMS
│   ├── locales/            # JSON файлы переводов
│   │   ├── ru.json
│   │   ├── lv.json
│   │   └── en.json
│   ├── reviews/            # Markdown файлы отзывов
│   ├── gallery-items/      # Markdown файлы галереи
│   ├── gallery/            # Изображения для галереи
│   └── proofs/             # Скриншоты отзывов
├── src/
│   ├── main.js             # Основной JS (только UI, без загрузки данных)
│   ├── scss/               # Стили
│   └── assets/             # Статические ресурсы
├── index.html              # Русская версия (шаблон)
├── lv/
│   └── index.html          # Латышская версия (шаблон)
├── en/
│   └── index.html          # Английская версия (шаблон)
└── vite.config.js          # 🆕 С SSG плагином
```

## 🔧 Конфигурация Decap CMS

Файл `public/admin/config.yml` содержит всю конфигурацию CMS:
- Backend (Git Gateway для Netlify)
- Коллекции (локализации, отзывы, галерея)
- Поля для редактирования

## 📤 Деплой на Netlify

1. **Подключите репозиторий к Netlify**
   - В Netlify Dashboard: `Add new site` → `Import an existing project`
   - Выберите ваш Git-репозиторий

2. **Настройте Build settings:**
   ```
   Build command: npm run build
   Publish directory: dist
   ```

3. **Включите Netlify Identity** (см. раздел выше)

4. **Готово!** Сайт автоматически деплоится при каждом пуше в main

## 🎨 Редактирование контента

### Через CMS (рекомендуется)
1. Зайдите на `/admin/`
2. Выберите нужную коллекцию
3. Редактируйте контент
4. Сохраните изменения

CMS автоматически создаст коммит в репозиторий, и Netlify пересоберет сайт.

### Через Git (для разработчиков)
1. Редактируйте JSON файлы в `public/locales/`
2. Создавайте/удаляйте MD файлы в `public/reviews/` и `public/gallery-items/`
3. Коммитьте и пушьте изменения

## 🌐 Переключение языка

Язык определяется автоматически по URL:
- `yourdomain.com/` → русский
- `yourdomain.com/lv/` → латышский
- `yourdomain.com/en/` → английский

Навигация между языками через кнопки в хедере.

## ⚙️ Разработка

### Добавление нового поля в локализацию

1. Обновите `public/admin/config.yml` (добавьте поле в секцию locale)
2. Обновите JSON файлы в `public/locales/`
3. Добавьте применение перевода в `src/main.js` (функция `applyTranslations`)

### Добавление новой коллекции

1. Добавьте коллекцию в `public/admin/config.yml`
2. Создайте функцию загрузки в `src/main.js`
3. Создайте функцию рендеринга контента

## 🔐 Безопасность

- CMS доступна только авторизованным пользователям через Netlify Identity
- Используйте "Invite only" для контроля доступа
- Git Gateway управляет правами на изменение репозитория

## 📞 Поддержка

Если возникнут вопросы или проблемы:
1. Проверьте настройки Netlify Identity
2. Убедитесь что Git Gateway включен
3. Проверьте консоль браузера на ошибки

---

**Let's Party** - Produced by Dynasty Events 🎉
